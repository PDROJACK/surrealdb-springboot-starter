# surrealdb-springboot-template
SurrealDB springboot starter kit

A simple todo app REST API using Surreal DB.

How to run:

1. Clone the repository.
3. Use docker compose to start SurrealDB.
2. Inside the root of repository directory. Run below command to start springboot app:
    ```mvn spring-boot:run```